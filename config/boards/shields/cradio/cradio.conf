# There is a known issue with Windows 
# failing to update the battery information after connecting to a ZMK keyboard. 
# You can work around this Windows bug by overriding a 
# Bluetooth config variable to force battery notifications even 
# if a host neglects to subscribe to them:
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
